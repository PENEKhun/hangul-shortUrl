version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ubuntu/app/zip
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu


hooks:
  ApplicationStart:
    - location: DeployScript.sh
      timeout: 10
      runas: ubuntu