version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ubuntu/app/zip
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu
hooks:
    AfterInstall:
    - location: bootScripts/initRun
      runas: ubuntu